/*
** EPITECH PROJECT, 2017
** Check_map.h
** File description:
** All maccros for check_map function
*/

#include <game.h>
#include <my.h>

#define ISValid(x) (((x) == '#') || ((x) == 'O') || ((x) == ' ') || ((x) == '\n') || ((x) == 'X') || ((x) == 'P'))
